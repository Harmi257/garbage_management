// src/domain/models/User.js
export class User {
    constructor(username, userEmail, mobile, city) {
      this.username = username;
      this.userEmail = userEmail;
      this.mobile = mobile;
      this.city = city;
    }
  }
  